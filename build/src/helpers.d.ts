import { Operation } from './types';
export declare function create_error_obj({ char, index, indexGen, indexExp, operation, }: {
    char: string;
    index: number;
    indexGen: number;
    indexExp: number;
    operation: Operation;
}): {
    char: string;
    index: number;
    indexGen: number;
    indexExp: number;
    operation: Operation;
};
